---
# Example credentials file for project-login skill.
# COPY this file to `credentials.local.mdc` and fill in real values there.
# `credentials.local.mdc` is git-ignored and must never be committed.
sites:
  - id: example-staging
    baseUrl: https://staging.example.com
    loginPath: /login
    username: your_test_user_email_here
    password: your_test_user_password_here
  - id: example-dev
    baseUrl: http://localhost:3000
    loginPath: /login
    username: your_local_test_user_email_here
    password: your_local_test_user_password_here
---

